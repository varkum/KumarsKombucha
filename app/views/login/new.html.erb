<%= form_with url: thisisvarun_path, class: "flex flex-col gap-y-4 px-6 lg:w-1/3 mx-auto mt-4" do |form| %>

  <%= form.label "Password" %>
  <%= form.password_field :password, class: "rounded" %>

  <%= form.submit "Submit", class: "rounded p-1 font-semibold bg-green-700 text-white" %>
<% end %>